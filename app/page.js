'use client'

// import useColorScheme from './hooks/useColorScheme';

export default function Home() {
  // const [colorScheme, setColorScheme] = useColorScheme()

  return (
    <>
      <main id='main'>
        <h1>STRIKE BACK</h1>

        <p>Donald Trump will ride this country into the ground if we do nothing. So what are we supposed to do to stop this?</p>

        <p>DO NOTHING.</p>
      
        <p>That's right, it's time to STRIKE. </p>

        <ul>
          <li>Do not pay rent.</li>
          <li>Do not go to work.</li>
          <li>Do not buy anything except food and water.</li>
        </ul>

        <button>Make the pledge</button>
      </main>
    </>
  );
}
